[project]
name = "ownfoil"
version = "2.4.0.dev1"
description = "Self-hosted Nintendo Switch library manager, with automated management tasks, multi user authentication, serving your library to multiple supported clients on your Switch."
readme = "README.md"
requires-python = ">=3.11"
license = "AGPL-3.0-or-later"
authors = [
    { name = "a1ex4", email = "12754559+a1ex4@users.noreply.github.com" },
]
keywords = ["atmosphere", "homebrew", "nintendo-switch", "self-hosted", "switch", "tinfoil"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Games/Entertainment",
]
dependencies = [
    "flask==3.1.3",
    "flask-login==0.6.3",
    "flask-migrate==4.1.0",
    "flask-sock==0.7.0",
    "flask-sqlalchemy==3.1.1",
    "gunicorn==26.0.0 ; sys_platform != 'win32'",
    "nstools==2.0.0b7",
    "nsz==5.0.0",
    "pyyaml==6.0.3",
    "requests==2.34.2",
    "setproctitle==1.3.7",
    "strawberry-graphql[flask]==0.324.0",
    "watchdog==6.0.0",
    "werkzeug==3.1.8",
    "zstandard==0.25.0",
]

[project.urls]
Homepage = "https://github.com/a1ex4/ownfoil"
Repository = "https://github.com/a1ex4/ownfoil"
Issues = "https://github.com/a1ex4/ownfoil/issues"

[project.scripts]
ownfoil = "ownfoil.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ownfoil"]

[tool.hatch.build.targets.wheel.force-include]
"app" = "ownfoil/_app"
